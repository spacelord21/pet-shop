import { styled } from "@shared/ui";

const Container = styled.div``;

type TFeedBackFieldProps = {
  title: string;
};

export const FeedBackField = ({ title }: TFeedBackFieldProps) => {
  return <Container></Container>;
};
